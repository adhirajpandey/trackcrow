export default function LandingPage() {
  return (
    <main className="placeholder-shell">
      <section className="placeholder-card">
        <p className="placeholder-eyebrow">TrackCrow</p>
        <h1>Frontend rewrite in progress.</h1>
        <p className="placeholder-copy">
          The previous product interface has been removed while the new frontend
          is rebuilt from scratch. Backend services and API routes remain in
          place during this transition.
        </p>
      </section>
    </main>
  );
}
