import { WorkspaceListPageSkeleton } from "@/components/product/page-loading-skeletons";

export default function RulesLoading() {
  return <WorkspaceListPageSkeleton actionWidths={["w-32"]} filterControlCount={3} />;
}
